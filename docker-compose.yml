services:
  fastapi:
    build: .
    ports:
      - "8001:8000"
    depends_on:
      - redis
    environment:
      - REDIS_URL=redis://redis:6379

  redis:
    image: redis:7
    ports:
      - "6378:6379"